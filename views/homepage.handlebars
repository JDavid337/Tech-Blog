
{{#each posts as |post| }}
<div class="row align-center mb-5">
  <a>SUP YALL</a>
  <div class="col-md-6">
    <h2>{{{get_emoji}}} {{post.title}}</h2>
  </div>
  <div class="col-md-6">
    <h3>{{post.context}}</h3>
  </div>
</div>
{{/each}}